export function RulesFaq() {
  return (
    <div className="rounded-2xl border border-gray-200 bg-white p-6">
      <h2 className="text-lg font-bold text-black">Rules & FAQ</h2>

      <div className="my-4 h-px bg-gray-100" />

      <div className="space-y-4">
        <div>
          <h3 className="font-bold text-black">Rules rules rules rules rules rules</h3>
          <p className="mt-2 text-sm text-muted-foreground">
            Rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules.
          </p>
        </div>

        <p className="text-sm text-muted-foreground">
          rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules.
        </p>

        <p className="text-sm text-muted-foreground">
          rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules.
        </p>
      </div>
    </div>
  )
}
