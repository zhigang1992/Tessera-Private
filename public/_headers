/sw.js
  Cache-Control: public, max-age=0, must-revalidate

# /manifest.webmanifest is served by functions/manifest.webmanifest.ts so the
# function sets its own headers (and bypasses Cloudflare's Bot Fight Mode,
# which otherwise 403s application/manifest+json requests from non-browser UAs).

/.well-known/assetlinks.json
  Content-Type: application/json
  Cache-Control: public, max-age=300
